{
  "name": "machi-list",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check assets/app.js && node --check scripts/google-places-sync.js && node --check scripts/generate-pages.js",
    "generate": "node scripts/generate-pages.js",
    "sync:places": "node scripts/google-places-sync.js"
  }
}
